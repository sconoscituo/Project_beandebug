services:
  db:
    image: postgres:15-alpine
    container_name: coffeelab_db_prod
    environment:
      POSTGRES_USER: ${DB_USER:-coffeelab}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-coffeelab_db}
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    networks:
      - coffeelab-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-coffeelab}"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: coffeelab_api_prod
    environment:
      - DATABASE_URL=postgresql://${DB_USER:-coffeelab}:${DB_PASSWORD}@db:5432/${DB_NAME:-coffeelab_db}
      - SECRET_KEY=${SECRET_KEY}
      - APP_NAME=Bean Debug
      - DEBUG=False
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-http://localhost}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - coffeelab-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: coffeelab_frontend_prod
    ports:
      - "80:80"
    depends_on:
      - api
    networks:
      - coffeelab-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -q --tries=1 --spider http://localhost:80/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres_data_prod:

networks:
  coffeelab-network:
    driver: bridge
